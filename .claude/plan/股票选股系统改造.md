# 股票选股系统改造项目 - 详细执行计划

## 📌 项目概要

| 项目属性 | 内容 |
|---------|------|
| **项目名称** | StockTrade Web (个人投研学习平台) |
| **技术方案** | 方案1：FastAPI + Vue3 前后端分离架构 |
| **技术栈** | 前端: Vue3 + TypeScript + Vite + Pinia + Element Plus + ECharts<br>后端: FastAPI + Pydantic + SQLAlchemy + Uvicorn<br>数据: SQLite + AKShare |
| **项目性质** | 个人投研学习，非商业化 |
| **核心目标** | Tushare → AKshare + Web可视化 |

---

## 🏗️ 项目结构设计

### 整体目录结构

```
StockTradebyZ-main/
├── backend/                          # FastAPI 后端
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                   # FastAPI 应用入口
│   │   ├── config.py                 # 配置管理
│   │   ├── dependencies.py           # 依赖注入
│   │   │
│   │   ├── api/                      # API 路由层
│   │   │   ├── __init__.py
│   │   │   ├── v1/                   # API v1 版本
│   │   │   │   ├── __init__.py
│   │   │   │   ├── stocks.py         # 股票数据接口
│   │   │   │   ├── strategies.py     # 选股策略接口
│   │   │   │   ├── backtest.py       # 回测接口
│   │   │   │   └── config.py         # 配置管理接口
│   │   │   └── deps.py               # 路由依赖
│   │   │
│   │   ├── core/                     # 核心业务逻辑
│   │   │   ├── __init__.py
│   │   │   ├── config.py             # 核心配置
│   │   │   ├── security.py           # 安全相关（预留）
│   │   │   └── logging.py            # 日志配置
│   │   │
│   │   ├── models/                   # SQLAlchemy 模型
│   │   │   ├── __init__.py
│   │   │   ├── database.py           # 数据库基类
│   │   │   ├── stock.py              # 股票模型
│   │   │   ├── kline.py              # K线数据模型
│   │   │   ├── strategy.py           # 策略配置模型
│   │   │   └── backtest.py           # 回测结果模型
│   │   │
│   │   ├── schemas/                  # Pydantic 模型（API 数据验证）
│   │   │   ├── __init__.py
│   │   │   ├── stock.py              # 股票相关 Schema
│   │   │   ├── kline.py              # K线数据 Schema
│   │   │   ├── strategy.py           # 策略 Schema
│   │   │   └── common.py             # 通用 Schema（响应格式等）
│   │   │
│   │   ├── services/                 # 业务服务层
│   │   │   ├── __init__.py
│   │   │   ├── akshare_service.py    # AKShare 数据服务
│   │   │   ├── strategy_service.py   # 选股策略服务（改造现有 Selector.py）
│   │   │   ├── data_service.py       # 数据管理服务
│   │   │   └── backtest_service.py   # 回测服务（可选）
│   │   │
│   │   ├── db/                       # 数据库操作
│   │   │   ├── __init__.py
│   │   │   ├── session.py            # 数据库会话管理
│   │   │   ├── base.py               # 数据库基类
│   │   │   └── init_db.py            # 数据库初始化脚本
│   │   │
│   │   ├── utils/                    # 工具函数
│   │   │   ├── __init__.py
│   │   │   ├── indicators.py         # 技术指标计算（复用现有代码）
│   │   │   └── helpers.py            # 辅助函数
│   │   │
│   │   └── tests/                    # 测试
│   │       ├── __init__.py
│   │       ├── conftest.py
│   │       ├── test_api/
│   │       └── test_services/
│   │
│   ├── requirements/                 # 依赖管理
│   │   ├── base.txt                  # 基础依赖
│   │   ├── dev.txt                   # 开发依赖
│   │   └── prod.txt                  # 生产依赖
│   │
│   ├── alembic/                      # 数据库迁移（可选）
│   │
│   ├── .env.example                  # 环境变量示例
│   ├── alembic.ini                   # Alembic 配置
│   └── pyproject.toml                # Python 项目配置
│
├── frontend/                         # Vue3 前端
│   ├── public/
│   │   └── favicon.ico
│   │
│   ├── src/
│   │   ├── assets/                   # 静态资源
│   │   │   ├── styles/
│   │   │   │   ├── variables.scss    # 全局变量
│   │   │   │   ├── mixins.scss       # SCSS mixins
│   │   │   │   └── global.scss       # 全局样式
│   │   │   └── images/
│   │   │
│   │   ├── components/               # 通用组件
│   │   │   ├── common/
│   │   │   │   ├── AppHeader.vue     # 顶部导航
│   │   │   │   ├── AppAside.vue      # 侧边栏
│   │   │   │   └── AppFooter.vue     # 底部
│   │   │   ├── charts/
│   │   │   │   ├── KLineChart.vue    # K线图表组件
│   │   │   │   └── IndicatorChart.vue # 指标图表
│   │   │   └── stock/
│   │   │       ├── StockCard.vue     # 股票卡片
│   │   │       └── StockList.vue     # 股票列表
│   │   │
│   │   ├── views/                    # 页面视图
│   │   │   ├── Dashboard.vue         # 仪表盘（首页）
│   │   │   ├── StockData.vue         # 数据管理
│   │   │   ├── StrategySelect.vue    # 选股策略
│   │   │   ├── StrategyConfig.vue    # 策略配置
│   │   │   ├── Backtest.vue          # 回测（可选）
│   │   │   └── Settings.vue          # 系统设置
│   │   │
│   │   ├── router/                   # 路由配置
│   │   │   └── index.ts
│   │   │
│   │   ├── store/                    # Pinia 状态管理
│   │   │   ├── index.ts
│   │   │   └── modules/
│   │   │       ├── stock.ts          # 股票数据状态
│   │   │       ├── strategy.ts       # 策略状态
│   │   │       └── app.ts            # 应用状态
│   │   │
│   │   ├── api/                      # API 调用
│   │   │   ├── client.ts             # Axios 客户端配置
│   │   │   ├── stock.ts              # 股票相关 API
│   │   │   ├── strategy.ts           # 策略相关 API
│   │   │   └── types.ts              # API 类型定义
│   │   │
│   │   ├── composables/              # Vue3 Composition API
│   │   │   ├── useChart.ts           # 图表相关逻辑
│   │   │   └── useStockData.ts       # 股票数据逻辑
│   │   │
│   │   ├── types/                    # TypeScript 类型定义
│   │   │   ├── stock.ts
│   │   │   ├── strategy.ts
│   │   │   └── index.ts
│   │   │
│   │   ├── utils/                    # 工具函数
│   │   │   ├── format.ts             # 格式化函数
│   │   │   └── validators.ts         # 验证函数
│   │   │
│   │   ├── App.vue                   # 根组件
│   │   └── main.ts                   # 应用入口
│   │
│   ├── index.html
│   ├── vite.config.ts                # Vite 配置
│   ├── tsconfig.json                 # TypeScript 配置
│   ├── tsconfig.node.json
│   ├── package.json
│   └── .env.development              # 环境变量
│
├── data/                             # 历史数据目录（兼容现有）
├── docs/                             # 项目文档
│   ├── 项目规范文档.md
│   ├── API文档.md
│   └── 数据库设计.md
├── .gitignore
├── README.md
├── docker-compose.yml
└── stocklist.csv
```

---

## 📊 数据库设计

### SQLite 表结构

#### 表1: stocks（股票基本信息）
```sql
CREATE TABLE stocks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ts_code VARCHAR(10) UNIQUE NOT NULL,
    symbol VARCHAR(6) NOT NULL,
    name VARCHAR(50) NOT NULL,
    market VARCHAR(10),
    industry VARCHAR(50),
    list_date DATE,
    is_active BOOLEAN DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 表2: kline_daily（日线K线数据）
```sql
CREATE TABLE kline_daily (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ts_code VARCHAR(10) NOT NULL,
    trade_date DATE NOT NULL,
    open DECIMAL(10,2),
    close DECIMAL(10,2),
    high DECIMAL(10,2),
    low DECIMAL(10,2),
    volume BIGINT,
    amount DECIMAL(20,2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(ts_code, trade_date)
);
```

#### 表3: strategies（选股策略配置）
```sql
CREATE TABLE strategies (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    class_name VARCHAR(50) UNIQUE NOT NULL,
    alias VARCHAR(50) NOT NULL,
    description TEXT,
    is_active BOOLEAN DEFAULT 1,
    config_json TEXT NOT NULL,
    sort_order INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 表4: selection_results（选股结果）
```sql
CREATE TABLE selection_results (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    strategy_id INTEGER NOT NULL,
    ts_code VARCHAR(10) NOT NULL,
    trade_date DATE NOT NULL,
    score DECIMAL(5,2),
    reason TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (strategy_id) REFERENCES strategies(id),
    UNIQUE(strategy_id, ts_code, trade_date)
);
```

#### 表5: data_update_log（数据更新日志）
```sql
CREATE TABLE data_update_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    data_type VARCHAR(20) NOT NULL,
    start_date DATE,
    end_date DATE,
    status VARCHAR(20) NOT NULL,
    records_count INTEGER DEFAULT 0,
    error_message TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 🔌 API 接口设计

### RESTful API 规范

#### 基础 URL
```
开发环境: http://localhost:8000/api/v1
```

#### 通用响应格式
```json
{
    "code": 200,
    "message": "success",
    "data": {...}
}
```

### API 端点清单

#### 1. 股票数据接口 (/api/v1/stocks)
| 方法 | 端点 | 功能描述 |
|------|------|----------|
| GET | `/stocks` | 获取股票列表 |
| GET | `/stocks/{ts_code}` | 获取股票详情 |
| GET | `/stocks/{ts_code}/kline` | 获取K线数据 |
| POST | `/stocks/sync` | 同步股票列表 |
| POST | `/stocks/{ts_code}/kline/sync` | 同步K线数据 |

#### 2. 选股策略接口 (/api/v1/strategies)
| 方法 | 端点 | 功能描述 |
|------|------|----------|
| GET | `/strategies` | 获取策略列表 |
| GET | `/strategies/{id}` | 获取策略详情 |
| PUT | `/strategies/{id}` | 更新策略配置 |
| POST | `/strategies/run` | 执行选股 |
| GET | `/strategies/results` | 获取选股结果 |

---

## 📐 项目规范制定

### Python 代码规范

#### 代码风格
- 遵循 PEP 8 规范
- 使用 Black 格式化（88字符行宽）
- 使用 isort 管理导入
- 使用 pylint 检查

#### 命名规范
```python
# 文件名：小写下划线
akshare_service.py

# 类名：大驼峰
class AKShareService:

# 函数/变量：小写下划线
def get_stock_list():

# 常量：大写下划线
MAX_RETRIES = 3
```

### Vue/TypeScript 代码规范

#### 代码风格
- 遵循 Vue 官方风格指南
- 使用 ESLint + Prettier
- TypeScript 严格模式

#### 命名规范
```typescript
// 文件名：大驼峰（组件）
KLineChart.vue

// 组件名：大驼峰
export default {
  name: 'KLineChart'
}

// 变量/函数：小驼峰
const stockCode = ref('000001.SZ')

// 常量：大写下划线
const MAX_RETRY_COUNT = 3
```

### Git 工作流规范

#### 分支策略
```
main（主分支）
  └── develop（开发分支）
       ├── feature/数据源替换
       ├── feature/Web可视化
       └── feature/策略配置管理
```

#### 提交规范
```
feat(backend): 添加AKShare数据同步接口

- 实现 stock_zh_a_hist 接口封装
- 添加数据库存储逻辑

Closes #1
```

---

## 🚀 分阶段实施计划

### 阶段一：项目初始化 + 数据层改造（3-5天）
- 后端项目初始化
- 数据库模型创建
- AKShare 服务层
- 策略服务层改造

### 阶段二：后端 API 开发（3-4天）
- Pydantic Schemas
- API 路由开发

### 阶段三：前端项目初始化（2-3天）
- Vue3 项目搭建
- 项目结构创建
- API 客户端配置

### 阶段四：前端页面开发（5-7天）
- Dashboard 页面
- K线图表组件
- 策略选股页面

### 阶段五：集成测试 + 优化（2-3天）
- 功能测试
- 性能优化
- 代码规范检查

### 阶段六：文档编写 + 部署（1-2天）
- 文档编写
- 部署配置

---

## 📦 依赖版本清单

### 后端依赖
```txt
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.10.0
sqlalchemy==2.0.36
akshare==1.14.0
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
python-dotenv==1.0.1
```

### 前端依赖
```json
{
  "dependencies": {
    "vue": "^3.5.0",
    "vue-router": "^4.4.0",
    "pinia": "^2.2.0",
    "element-plus": "^2.8.0",
    "echarts": "^5.5.0",
    "axios": "^1.7.0"
  }
}
```

---

**总预计时间：16-24天**
